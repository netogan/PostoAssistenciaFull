@model IEnumerable<PostoAssistenciaFull.Models.ChamadaTrabalhador>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Chamada.Observação)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trabalhador.NomeCompleto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Observação)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Chamada.Observação)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trabalhador.NomeCompleto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Observação)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ChamadaTrabalhadorId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ChamadaTrabalhadorId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ChamadaTrabalhadorId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
